{
  "name": "snmpcanvas",
  "version": "0.1.0",
  "description": "Lightweight self-hosted SNMP monitoring: poll SNMPv2c/v3 devices, store history in SQLite, view it in a dependency-free web UI.",
  "license": "Unlicense",
  "type": "commonjs",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "mock-agent": "node tools/mock-agent.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "net-snmp": "^3.20.0"
  }
}
